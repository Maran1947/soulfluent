services:
  frontend:
    build: .
    restart: unless-stopped
    env_file:
      - ./.env.local
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
